# Fixed-duration sampling config for Cairo dataset - duration 60 seconds, presegmented

dataset_name: "cairo"
raw_data_path: "data/raw/casas/cairo"
output_dir: "data/processed/casas/cairo/FD_60_p"

strategy: "fixed_duration"

# Duration windows to generate (in seconds)
duration_seconds: [60]
min_events_per_window: 3
max_events_per_window: null
max_sequence_length: 256

# Train/test split
split_strategy: "random"
train_ratio: 0.8
random_seed: 42

# Overlap settings
overlap_factor: 0.5

# Presegmentation
use_presegmentation: true
presegment_label_level: "l1"
min_segment_events: 1
exclude_no_activity: true

# Filtering
filter_numeric_sensors: true
max_gap_minutes: null

# Metadata preservation
preserve_full_metadata: true
include_spatial_info: true
include_sensor_types: true

# Processing limits
max_lines: null
max_windows: null


