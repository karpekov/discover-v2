{
  "comment": "SCAN clustering training on milan_baseline_50 model with 20 clusters - FULL MODEL FINE-TUNING (encoder + head)",

  "num_clusters": 20,
  "dropout": 0.1,
  "freeze_encoder": false,

  "batch_size": 32,
  "learning_rate": 5e-5,
  "weight_decay": 1e-4,
  "betas": [0.9, 0.999],
  "max_epochs": 20,
  "grad_clip_norm": 1.0,

  "entropy_weight": 2.0,
  "num_neighbors": 20,

  "sequence_length": 50,
  "embedding_batch_size": 128,
  "num_workers": 0,

  "log_interval": 50,
  "save_interval": 1000,

  "pretrained_model_path": "src-v2/trained_models/milan_baseline_50/best_model.pt",
  "train_data_path": "data/data_for_alignment/milan_training_50/milan_train.json",
  "vocab_path": "data/data_for_alignment/milan_training_50/milan_vocab.json",
  "output_dir": "src-v2/trained_models/milan_scan_20clusters_finetune",

  "use_wandb": true,
  "wandb_project": "discover-scan",
  "wandb_name": "milan_scan_20clusters_finetune",
  "wandb_tags": ["milan", "scan", "clustering", "20-clusters", "full-finetune", "baseline-50"],
  "wandb_notes": "SCAN clustering training on milan_baseline_50 pre-trained model with 20 clusters. FULL MODEL FINE-TUNING (encoder + clustering head). Uses KNN mining with 20 neighbors and entropy weight 2.0. Lower LR (5e-5) and smaller batch size (32) for stable fine-tuning.",
  "wandb_group": "milan_scan"
}
